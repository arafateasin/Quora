<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= username %>'s Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <nav class="bg-white shadow sticky top-0 z-10">
        <div class="container mx-auto flex justify-between items-center p-4">
            <div class="flex items-center">
                <span class="text-xl font-bold text-red-700">Quora</span>
            </div>
            <div class="flex items-center">
                <% if (user) { %>
                    <span class="mr-4 text-gray-700">Welcome, <%= user.username %></span>
                    <a href="/posts/new" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">Add Question</a>
                    <a href="/logout" class="ml-4 bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600 transition">Logout</a>
                <% } else { %>
                    <a href="/signup" class="bg-red-700 text-white px-4 py-2 rounded hover:bg-red-600 transition">Sign Up</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-4">
        <h1 class="text-maroon text-4xl font-bold mb-6"><%= username %>'s Posts</h1>
        <% if (userPosts.length === 0) { %>
            <p class="text-gray-500">No posts available from this user.</p>
        <% } else { %>
            <% for(post of userPosts) { %>
                <div class="bg-white p-4 mb-4 rounded-lg shadow-lg">
                    <h4 class="text-xl font-semibold mb-2"><%= post.title %></h4>
                    <p class="text-gray-700 mb-4"><%= post.content %></p>
                    <div class="flex justify-between text-sm text-gray-500">
                        <span>Upvote: <%= post.upvotes || 0 %></span>
                        <span>Comment: <%= post.comments || 0 %></span>
                    </div>
                </div>
            <% } %>
        <% } %>
        
        <br>
        <a href="/posts" class="bg-red-700 text-white px-4 py-2 rounded hover:bg-red-600 transition">Back to Posts</a>
    </div>
</body>
</html>